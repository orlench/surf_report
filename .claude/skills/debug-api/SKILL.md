# Debug API Integration
1. Identify the API endpoint and auth method
2. Test the raw endpoint with curl first: `curl -v <endpoint>`
3. Check environment variables are set (print names only, not values)
4. Only then look at application-level code
5. Report findings step by step
